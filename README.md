# GEM Dashboard

> Dashboard for Gemini GEM. 

Gemini 的 GEM 页面在子页面下，用起来很不方便，总是需要单独点进去，还要等网页刷新。

其实每个 GEM 的链接是固定的，只需要把他们存在一个书签里就好。

直接用 Gemini 做一个漂亮的导航页面就十分方便了。

只使用简答的前端技术就可以完成这个静态网页。

使用 yaml 读入书签数据方便用户添加新的 GEM 链接。
